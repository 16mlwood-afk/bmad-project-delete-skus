# Core dependencies for SKU Cleanup Tool
boto3==1.28.0              # AWS SDK for Python (SP-API)
pandas==2.0.0              # Data processing and TSV parsing
python-dotenv==1.0.0       # Environment variable management
requests==2.31.0           # HTTP requests for API calls
tabulate==0.9.0            # Table formatting for reports

# Optional: For better logging and CLI interface
colorlog==6.7.0            # Colored console logging
click==8.1.7               # CLI framework (if we add CLI features)
tqdm==4.65.0               # Progress bars

# Optional: For testing
pytest==7.4.0              # Unit testing framework
pytest-mock==3.11.1        # Mocking for tests
